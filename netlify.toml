[build]
  # מתקין תלויות רגילות, משלים חבילות חסרות אוטומטית, ורק אז בונה
  command = "npm ci && node scripts/auto-install-missing.cjs && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.11.1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
