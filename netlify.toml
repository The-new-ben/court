[build]
  # מתעלמים מה-lock הישן, מתקינים מחדש, ואז בונים
  command = "rm -f package-lock.json && npm install --no-audit --no-fund && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.11.1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
